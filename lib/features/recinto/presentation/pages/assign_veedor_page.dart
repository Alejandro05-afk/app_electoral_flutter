import 'package:flutter/material.dart';

class AssignVeedorPage extends StatelessWidget {
  const AssignVeedorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Asignar Veedor')),
      body: const Center(child: Text('Asignación de veedor a mesa')),
    );
  }
}
